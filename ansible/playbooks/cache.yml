---
- name: Apply cache host baseline
  hosts: cache
  become: true
  roles:
    - role: baseline
      vars:
        # Do not point cache hosts at their own VIP while Squid is being built.
        baseline_package_proxy_url: "{{ cache_upstream_proxy_url | default('') }}"
    - cache_proxy
